export class BranchInfoModel {
  /***
   * کد شعبه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_code: string;
  /**
   * کد سرپرست
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  sar_code: string;
  /**
   * کد حوزه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  hozeh_code: string;
  /**
   *نام حوزه
   *می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  hozeh_name: string;
  /**
   * نام شعبه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_name: string;
  /**
   * نام شعبه انگلیسی
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_english_name: string;
  /**
   * نام سرپرست
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  sar_name: string;
  /**
   * کد محل کار
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  work_place_code: number;
  /**
   * نام محل کار
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  work_place_name: string;
  /**
   * رتبه محل کار
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  work_place_level: string;
  /**
   * کد شعبه مربوط به باجه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  base_branch_code: string;
  /**
   * آدرس شعبه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_address: string;
  /**
   * آدرس شعبه انگلیسی
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  english_branch_address: string;
  /**
   * تلفن شعبه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_phone: string;
  /**
   * فکس شعبه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_fax: string;
  /**
   * ایمیل شعبه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_email: string;
  /**
   * کد پستی شعبه
   * می تواند null ، صفر یا مقادیر نامعتبر باشد
   */
  branch_postal_Code: string;
}
